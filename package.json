{
  "name": "obsidian-memory-plugin",
  "version": "0.4.2",
  "private": true,
  "description": "The Obsidian Memory skill packaged for OpenClaw, Codex, and Hermes.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22.22.3" },
  "files": [
    "index.js",
    "__init__.py",
    "plugin.yaml",
    "lib",
    "skills",
    "scripts",
    "openclaw.plugin.json",
    ".codex-plugin",
    "README.md",
    "SOURCES.md",
    "examples"
  ],
  "openclaw": {
    "extensions": ["./index.js"],
    "runtimeExtensions": ["./index.js"]
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check lib/config.js && node --check lib/prompt.js && node --check scripts/setup-codex.mjs && node --check scripts/setup-hermes.mjs && node --check scripts/setup-openclaw.mjs && python3 -m py_compile __init__.py",
    "check:codex": "node scripts/validate-codex-plugin.mjs",
    "setup:codex": "node scripts/setup-codex.mjs",
    "setup:hermes": "node scripts/setup-hermes.mjs",
    "setup:openclaw": "node scripts/setup-openclaw.mjs",
    "prepack": "npm run check && npm run check:codex && npm test"
  }
}
